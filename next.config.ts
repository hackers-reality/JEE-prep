import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Tauri/Capacitor static export config will be added in Phase 7
  // During Phases 1-6, keep standard server-mode for development
};

export default nextConfig;
